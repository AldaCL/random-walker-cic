# This file will contain parametrizable constants for the project
# At the beginning of the project, we can allow to set these constants at 
# module level, later, we can move these to a configuration file, or to specific classes

from enum import Enum


class PROBABILITY_DISTRIBUTIONS(Enum):
    """
    As long as the project envolve working 
    with some of the common probability distributions,
    we can define them here
    """
    pass